export type Bounds = {
  top: number; // y_max
  right: number; // x_max
  bottom: number; // y_min
  left: number; // x_min
  front: number; // z_max
  back: number; // z_min
};

export type Point = {
  x: number;
  y: number;
  z: number;
};
